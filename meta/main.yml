galaxy_info:
  author: Development Gateway
  description: Set up a two-node replicating Postgres cluster
  company: Development Gateway
  license: GPLv3
  min_ansible_version: 2.5
  platforms:
    - name: EL
      versions:
        - 7

  galaxy_tags:
    - postgresql
    - postgres
    - cluster
    - replication
    - database

dependencies:
  - devgateway.vendor-postgres
